PLAN_DECISION: PASS

### 已确认可实现部分
1. **核心机制与数据边界**：方案选择唯一的“风险调整动量”改进，仅修改 `decide` 方法内 `for lookback in (20, 40, 60):` 循环体中的 `raw` 计算逻辑。新增计算严格使用 `context.closes` 截至 `signal_i` 的切片，无未来函数，符合日频/固定时点模式的时序约束。
2. **接口与资源约束**：未新增外部数据接口、未扩大股票池、未引入新参数。使用的 `np.where` 与 `np.nanstd` 均基于已导入的 `numpy`，资源增量评估合理，未触碰安全上下文以外的限制。
3. **保留项明确**：方案清晰列出了仓位控制、行业约束、过滤条件等保持不变的模块，未重写无关逻辑，符合增量改进规则。
4. **防除零处理**：方案通过 `np.where(vol > 1e-6, vol, np.nan)` 实现了除零保护，当波动率过低时强制令得分为 `np.nan`，后续的 `_percentile` 会将其自然排除，不会触发运行时异常或阻断交易链路。

### 工程实现必做项（交由后续代码审核验证）
1. **缺失值传播与异常降级**：当某只股票在 `lookback` 窗口内存在停牌等导致的 `np.nan` 收盘价时，`np.nanstd` 将返回 `nan`。需确保该 `nan` 能安全穿透 `np.where` 并在后续的 `_percentile` 排序中被 `np.isfinite` 正确剔除，不产生类型异常或导致整体 `scores` 退化，从而避免触发“整轮静默零交易”。
2. **极低波动率的退化验证**：虽然设置了 `1e-6` 保护，但需在本地回测中确认在极端窄幅震荡市下，该阈值过滤不会导致有效股票池大幅缩减至 `< TOP_K`，从而引发 `_choose_desired` 逻辑异常。
3. **原 `raw` 赋值的完整替换**：工程师在实施时需确保原基线中的 `raw[~eligible | ~np.isfinite(old) | (old <= 0)] = np.nan` 逻辑在新的 `raw = raw_ret / np.where(...)` 代码块后依然被正确执行，以保持非合格股票的得分清洗逻辑不变。
