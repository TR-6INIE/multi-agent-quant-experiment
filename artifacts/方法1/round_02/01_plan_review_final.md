PLAN_DECISION: PASS

# 基本工作：信号时间链与方案可行性审核

## 1. 信号完整时间链追踪
- **数据时点**：`signal_i = len(closes) - 1` 代表 T 日。方案提取 `breadths[0]` 即 `offset=0` 时的单日宽度，严格对应 T 日收盘后的截面数据。`breadth_3d` 基于 T-2 至 T 日，`breadth_1d` 基于 T 日，时间链清晰且无未来函数。
- **执行边界**：信号在 T 日生成，订单在 T+1 日 09:35 处理，同一根K线不兼具信号与成交，完全符合固定时点模式接口。

## 2. 接口与数据限制验证
- **改动范围**：方案仅修改 `decide` 方法中的条件分支，未新增外部文件、网络、财务数据或成分历史请求。
- **数据依赖**：`breadths[0]` 取自基线已计算的循环内数据，未新增 SafeStrategyContext 字段。
- **非空防退**：方案明确包含 `breadths[0] if breadths else 0.0`，当历史数据不足（如 `t < 59` 导致列表为空）时安全降级为 0.0，不会触发异常或静默空仓。

## 3. 参数与过拟合风险识别
- **参数自由度**：方案复用基线既有阈值（0.50与0.58），未引入新参数。通过将入场阈值从 `breadth_3d` 切换至 `breadth_1d`，改变了触发机制但未增加寻优维度，属于低过拟合风险改进。
- **阈值物理意义**：非对称设计使出场依赖中期平滑（防噪音），入场依赖单日突破（防滞后），逻辑自洽。

## 4. 既有模块未改动确认
基线的选股因子、过滤条件、行业约束等保持不变。由于改动仅影响择时分支判断，与 `_choose_desired` 等组合构建函数无异常交互。

---

# 绩效工作：创新机制与过拟合验证

## 1. 实质创新判断
- **核心机制**：绩效提案提出“基于行业动量分位数的动态行业集中度控制”。其本质是将基线的静态截面分散约束，转化为依赖行业动量相对排名的状态依赖型约束。
- **创新性**：引入行业横截面动量作为集中度放大器，并辅以行业内 eligible 股票数量 ≥ 3 的微观流动性兜底，这并非简单参数平移，而是对组合构建规则的结构性改变，具备实质创新性。

## 2. 过拟合验证办法
针对该绩效提案，提出以下可证伪验证方法：
- **样本外一致性测试**：在评价期内，统计触发 `MAX_PER_INDUSTRY = 2` 的行业，计算其触发后5日的平均收益。若该收益未显著优于全市场等权基准，则说明行业动量预测力失效，集中度放大无益。
- **极端回撤归因**：要求记录每次持有2只同行业股票期间的组合波动率。若组合最大回撤超过基线设定的12.7%阈值，且主因归于该双持行业反转，则判定动态集中度机制无效。

## 3. 下一轮代码审核关注点
若该绩效提案进入代码实现，需重点审核：
- 行业动量计算的截面宽度（当 eligible 股票极少时，分位数易失真，需设最小样本门槛）。
- 动态参数变化与 `_choose_desired` 遍历逻辑的交互（避免行业限制提升后，持仓超限或权重溢出）。

---
**结论**：基本工作改进方案（非对称入场确认）逻辑严密，接口完全可由现有模板实现，判定 PASS。绩效工作提案已提供可验证的创新点与防过拟合方案。
