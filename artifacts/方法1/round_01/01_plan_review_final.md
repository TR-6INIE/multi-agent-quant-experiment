PLAN_DECISION: PASS

# 基本工作

## 方案一致性与接口审核
选定改进为“Breadth 3日平滑”，修改范围仅涉及 `decide` 方法内 `breadth` 计算逻辑，未新增数据接口、未扩大股票池、未提高频率，符合单次核心改进限制。

## 数据时点与未来函数
交付代码中循环变量 `t = signal_i - offset`，切片 `closes[t-19:t+1]` 和 `closes[start_idx:t+1]` 均满足 `t <= signal_i`。`signal_i` 为上一交易日索引，信号与执行之间无同一K线既决又成交的问题，无未来函数。

## 输入边界与异常路径
1. `t < 59` 时 `break`，保护了 `closes[t-19:t+1]` 的下标边界。
2. `np.any(valid_t)` 为 False 时回退 `b = 0.0`，最终 `breadths` 为空时回退 `0.0`，不会抛异常或静默空仓。
3. `ready` 要求 `len(calendar) >= 75`，而 `signal_i` 至少为 74，足够覆盖 `t-59` 的回溯需求。

## 工程实现必做项（交后续代码审核验证）
- 确保 `_column_mean` 在全 NaN 列输入下返回 NaN 且不抛警告（基线已有 `where` 保护，改动不涉及此函数）。
- 确保替换后 `eligible` 计算块仍使用单日 `ma20`（原逻辑），不受平滑循环影响；研究员描述“移除原有单日 breadth 计算逻辑”不应误删 `ma20` 变量。

# 绩效工作

## 结构比较与信号重复识别
基线核心结构为：截面动量打分 + 趋势/流动性过滤 + 市场宽度择时 + 行业中性约束。

绩效提案（动态行业集中度）与基本工作选定改进（Breadth 平滑）是两个独立的核心机制变更，违反“每轮最多1项”规则，因此绩效提案本轮不纳入实施，仅作结构审查。

## 创新性判定
动态行业集中度提案本质上是将基线静态常量 `MAX_PER_INDUSTRY` 替换为“行业动量排名 → 条件上限”的状态依赖映射。其输入数据（`closes`、`industries`）与基线完全相同，计算逻辑（行业均值截面排名）属常规截面操作，未引入新数据源或新研究规则，不构成结构性创新。若未来实施，需作为独立轮次提交，并明确行业动量窗口与排名阈值的参数自由度约束。

## 已确认可实现部分
基本工作的 Breadth 3日平滑方案：逻辑闭环、边界安全、无未来函数、资源无增量、接口无变更，可交由工程师按交付清单实现。
