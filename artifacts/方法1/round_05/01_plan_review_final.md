PLAN_DECISION: PASS

## 基本工作审计

### 一、接口与数据时点验证
1. **数据可行性**：动态 `KEEP_BUFFER` 所需的截面波动率完全基于 `closes[signal_i - 20 : signal_i + 1]` 计算，属模板已提供的日频历史字段，无新增数据请求。
2. **无未来函数**：信号基于 T 日收盘数据，订单 T+1 执行，符合时点对齐要求。
3. **模板约束**：未修改 `SafeStrategyDecision` 调用接口，未触及股票池或外部数据，满足唯一核心改进限制。

### 二、经济逻辑与系统性风险检查
1. **消融与归因闭环**：方案落实了上轮要求的旧/新规则逐信号日共同触发统计，且在实施前对静态(2.0)与动态(3.0)缓冲进行了理论消融推演，逻辑自洽。
2. **状态退化风险**：当市场短期波动率飙升触发 `HIGH_VOL_KEEP_BUFFER=3.0` 时，持仓保留阈值放宽至前15名。此机制虽能减少碎单，但在阴跌高波动市中可能导致持仓死抱弱势股。方案已在“失效场景”中识别此风险，且基线原有的 `breadth_3d < 0.50` 或 `breadth_1d < 0.40` 出场规则仍生效，提供了系统性止损底线。

### 三、参数自由度与过拟合风险
新增6个常量，但参数来源均有文献或统计惯例支撑。`VOL_SPIKE_RATIO=1.2` 对应周度与月度波动率比值的经验阈值，非依回测收益倒推。

### 四、代码实现审查
1. **除零与边界安全**：`returns` 计算中 `closes[signal_i - VOL_LONG_WINDOW:signal_i]` 作为分母，当价格为0或NaN时会产生异常。方案已添加 `valid_ret` 掩码及 `vol_long > 0` 校验，逻辑闭环。
2. **函数签名重构**：将全局 `KEEP_BUFFER` 替换为函数参数 `keep_buffer` 传递至 `_choose_desired` 和 `_choose_kept`，不改变原有行业过滤与排名机制，改动局部且安全。

## 绩效工作审计

### 一、原创性与独立性
绩效提案“基于持仓宽度的假突破防护”仅作为工程笔记文档化，未合并入运行代码，符合“每轮最多1项核心机制”的实验约束。其利用 `context.selected_held` 与 `closes` 计算持仓微观健康度，避免了对外部净值数据的依赖，具有独立创新性。

### 二、系统性风险识别
绩效提案指出全市场 `breadth_3d` 存在“平均数幻觉”。若按绩效提案实施（强制降仓至 `TOP_K=2`），在宽幅震荡市中持仓个股频繁上下穿越均线会导致策略反复降仓加仓，放大摩擦成本。方案已明确此失效场景并设定证伪条件，风险可控。

## 工程实现必做项
以下交由后续代码审核验证，不作为本轮退回理由：
1. 确保 `returns` 计算中 `closes[signal_i - VOL_LONG_WINDOW:signal_i]` 的分母为0或含NaN时，`np.diff` 不会产生污染性异常值，掩码过滤需严格覆盖。
2. 确保 `_choose_desired` 和 `_choose_kept` 函数签名更新后，所有内部引用的 `keep_limit` 计算均正确使用传入的 `keep_buffer` 参数。
3. 代码中需确保 `eligible` 数组在传入波动率计算前已计算完成，且两者基于同一 `signal_i` 对齐。

**结论**：方案核心改动清晰、数据依赖合规、无未来函数，消融推演与归因纠正已落实。可进入代码实现与冻结阶段。
