PLAN_DECISION: PASS

方案通过预审。已确认可实现部分及后续工程实现注意事项如下：

**1. 核心修改审查**
将 `is_bear_regime = (breadth < EXIT_BREADTH) or (breadth < breadth_ma)` 修改为 `is_bear_regime = (breadth < EXIT_BREADTH)` 的方案，语义明确且符合安全约束。未改变核心数据接口、未增加资源开销、未引入未来函数风险。

**2. 保留项审查**
方案明确保留 `breadth_ma` 的历史循环计算逻辑及 `is_bull_regime` 的双重确认条件（`breadth >= 0.58 AND breadth > breadth_ma`）。这一保留是合理的，因为进场逻辑仍依赖 `breadth_ma`，且避免了无效代码的清理。所有保留项均未超出 `SafeStrategyContext` 字段约束，数据时点仍为上一交易日。

**3. 异常路径与空仓风险审查**
修改后，当 `breadth` 处于 0.50 至 0.58 之间时，策略将进入过渡态（`_choose_kept` 保持现有持仓）。方案已正确识别到如果此前因 `breadth < 0.50` 触发清仓（持仓为空），在市场反弹但未达到 0.58 进场阈值前，策略将处于“空仓-保持空仓”状态。方案研究意图（容忍一定空仓期以避免高位洗盘）与研究规则一致，不属于“异常静默空仓”。

**4. 工程实现必做项（交由后续代码审核验证）**
- 交付工程师清单要求“仅修改 `is_bear_regime` 赋值，保持其余代码完全不变”可精确执行。研究员无需在此环节进一步明确状态复位或降级路径，因为基线已具备完整的过渡态与Fallback处理（`_choose_kept`）。
- 基线中 `breadth < breadth_ma` 隐含对 `breadth_ma` 为 NaN 的容忍度（NaN比较为False，即不会因NaN误清仓）。移除该分支后，`EXIT_BREADTH` 为固定常数 0.50，不存在除零或 NaN 传播导致的异常阻断风险。

**5. 伪证检查**
方案中引用的8月28日“breadth=0.71触发清仓”案例及文献支持，均基于模拟决策日期（2025-09-30）及之前的数据，未使用评价区间数据倒推参数，未进行过度拟合（未修改任何参数值）。
