PLAN_DECISION: PASS

**审核结论**

方案选择将全局常量 `KEEP_BUFFER` 从 `2.0` 修改为 `3.0`，作为本轮唯一核心改进，符合“每轮最多1项核心机制改进”的限制。此改动在计算逻辑上会使 `_choose_desired` 和 `_choose_kept` 函数中的 `keep_limit` 从 10 放宽至 15，直接作用于持仓保留优先级的筛选，不影响其他模块。

**已确认可实现的部分**

1. **接口与数据兼容性**：改动不涉及任何新增数据接口，完全依赖现有 `SafeStrategyContext` 提供的 `closes`、`amounts`、`codes`、`industries` 和 `selected_held`。数据时点仍为上一交易日，无未来函数风险。
2. **计算资源**：仅修改一个全局浮点数常量，不增加内存或计算资源消耗。
3. **代码逻辑安全性**：`keep_limit` 的计算公式 `max(TOP_K, int(math.ceil(TOP_K * KEEP_BUFFER)))` 对于 `3.0` 的输入完全有效，计算结果为 15，不会引发越界或异常路径。`KEEP_BUFFER` 仅在持仓保留逻辑中被调用，不改变动量得分计算、市场宽度风控及行业中性约束的核心信号规则。
4. **承接持仓与资金**：方案明确承接期初资产及既有持仓，符合跨季度连续运行要求。

**工程实现必做项（交由后续代码审核验证）**

1. 确保修改仅限于全局变量 `KEEP_BUFFER` 的赋值，且 `TOP_K` 保持为 5 不变。
2. 确保非对称市场宽度风控（包括空头清仓、多头建仓、过渡保持）及 10 日 `breadth_ma` 计算循环保持原样，未被修改。
