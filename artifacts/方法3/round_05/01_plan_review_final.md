PLAN_DECISION: PASS

**已确认可实现的部分：**

1. **核心改进单一且明确**：方案仅修改空头清仓条件，将 `is_bear_regime` 从 `(breadth < EXIT_BREADTH)` 扩展为 `(breadth < EXIT_BREADTH) or (breadth < (breadth_ma - BREADTH_MA_BUFFER))`，并新增常量 `BREADTH_MA_BUFFER = 0.05`。这属于核心风控规则的改变，未重写无关模块。

2. **接口与数据合规**：方案使用日频/固定时点模式，依赖的 `context.closes`、`context.amounts` 均为截至上一交易日的数据。`breadth_ma` 在基线中已通过历史循环计算得出，本轮仅调用该既有变量参与新布尔判断，未引入任何新的数据切片、外部文件或未来函数。

3. **逻辑无阻断**：当 `is_bear_regime` 触发时返回空仓 `tuple()`，随后由冻结撮合引擎处理降级与卖出，不存在导致整轮静默零交易的数据链路缺陷或异常静默路径。

4. **资源增量极小**：仅增加一次浮点减法与布尔比较，无内存或计算资源越界风险。

**一般问题（交由后续代码审核验证，不阻断本轮预审）：**

- **位置与影响**：方案在“交付工程师清单”第4点提出了“重要工程修复”，建议在引擎侧落盘 trades.csv 时对 NaN 的 score 进行兜底，或在策略侧对 ineligible 股票赋予极小值。
- **修改办法**：冻结撮合引擎及 trades.csv 落盘逻辑不属于策略模块的修改范畴，策略侧不得自行实现账户或输出逻辑。由于 ineligible 股票 score 为 NaN 是基线既有实现且不影响策略信号，建议将此项降级为纯数据审计建议，不作为本轮代码改动项。若需在策略侧修改，须确保不破坏 `scores[~eligible] = np.nan` 对截面排名的过滤作用。
