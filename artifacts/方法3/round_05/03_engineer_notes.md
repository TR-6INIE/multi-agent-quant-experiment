A-001 修复说明：
- 审核指出 `signal_i < 60` 的提前返回分支中，`scores` 仍为初始全 NaN 数组，未进行 -1.0 兜底，会导致 trades.csv 产生空 score 字段。
- 已在提前返回分支内加入与主返回路径一致的兜底处理：`scores = np.where(np.isfinite(scores), scores, -1.0)`。
- 该修改仅解决审核列明的数据审计盲区，未改变任何研究逻辑、选股规则、风控阈值或参数。
- 其他非阻塞建议（BREADTH_MA_BUFFER 的过拟合风险关注、参数自由度）未做修改，维持原获批研究方案。
- 未声称通过冻结本地回测；仅针对审核报告中的阻断问题进行了局部修复。
