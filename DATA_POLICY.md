# 数据公开边界

## 本仓库不提供的数据

以下内容没有进入公开副本，也被`.gitignore`明确排除：

- 股票、指数的一分钟、五分钟和日线行情；
- QMT `DAT`、`DividData`、板块和证券基础资料；
- 财务报表、公告日事件和时点财务缓存；
- 股票池及申万一级行业成分文件；
- 开发期和评价期的逐日权益、逐笔成交、订单、每日目标/实际持仓；
- 初始或期末账户持仓明细；
- API密钥、本机模型配置、API完整请求日志；
- 任何`.npz/.npy/.DAT/.parquet/.pkl`缓存。

仓库中的CSV只包含按轮或按完整区间聚合后的收益、回撤、成交数量、代码哈希与部署状态，不包含价格序列和财务字段值。

## 用户自行提供的数据接口

日频市场缓存为NPZ，至少包含：

| 字段 | 形状 | 含义 |
|---|---|---|
| `calendar` | `(T,)` | 升序交易日，整数`YYYYMMDD` |
| `codes` | `(N,)` | 股票代码，如`600000.SH` |
| `opens` | `(T,N)` | 冻结Broker使用的执行价格 |
| `closes` | `(T,N)` | 历史信号和估值使用的收盘价 |
| `amounts` | `(T,N)` | 日成交额 |

缺失价格使用`NaN`，各矩阵必须与`calendar × codes`严格对齐。财务缓存需要保存证券、字段、报告期、公告日、可得日和值；实验程序只向策略提供上一交易日之前已经公告的数据。

分钟策略还需要QMT兼容的一分钟数据源或自行实现同等接口。当前代码不会联网下载市场数据。

## 偏差披露

原实验使用固定当前股票池和固定当前申万一级行业快照，因此存在幸存者偏差和行业分类前视偏差。公告日时点财务处理与严格的信号/执行时间隔离可以减少直接未来数据泄漏，但不能消除固定快照、策略事后选择和多轮反馈造成的偏差。

数据使用者必须自行确认其行情、财务和指数数据的授权范围，不应把第三方商业数据提交到公开仓库。
