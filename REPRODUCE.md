# 复现实验说明

本公开仓库不附带原始行情、财报、指数、行业快照、逐日净值或逐笔成交，因此不能开箱精确复算归档收益。

如需使用自有数据重跑，请复制 `管理员/config/*.example.json` 为对应本地配置，设置模型API环境变量，并按照 `DATA_POLICY.md` 准备本地缓存。由于历史模型服务、固定快照和原始缓存未公开，重跑属于协议级复现，不保证数值级复现。

归档结果的权威聚合表位于 `results/`，每轮冻结代码位于 `artifacts/`，第五轮策略位于 `strategies/`。
